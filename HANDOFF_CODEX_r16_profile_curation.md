# HANDOFF — CODEX: R16 · 프로필 큐레이션 유실 + 게이트 출력 델타

**작성일:** 2026-07-12
**출처:** `DEBATE_e1_round1.md` Round 6 (R16)
**선행 상태:** E1 계측 ✅ 종결 (R1~R15). 투자가능성 게이트 배선 ✅ 완료 (R12·R13).
**이 문서의 범위:** **E1이 아니다.** R12 수정이 드러낸 **프로필 데이터 무결성** 문제다.

---

## 0. 한 줄 요약

**R12로 게이트를 켜기 전까지, `multiple: 10.0` / `peers: []` 짜리 자동 생성 스텁 프로필이 확신 있는 밸류에이션을 내고 있었다. NVDA를 포함해서.**

---

## 1. 사실 관계 (코드·파일 직접 확인)

### 1-1. 게이트가 죽어 있었다 (R12 — 이미 수정됨)

`valuation_runner._apply_investability_gate`가 `gate_inputs_from_profile(..., text="")`를 넘기고 있었다.
→ `_check_no_todo_markers("")` **항상 통과**, placeholder 검출(`mult == 10.0 and "todo" in text`) **절대 발화 안 함**.
→ `engine/investability_gate.py`가 스스로 선언한 5개 blocking check 중 **2개가 구조적으로 실패 불가능**했다.
**수정 완료:** `text=vi.profile_text` (`schemas/models.py:833`에 `profile_text: str = Field(default="", exclude=True, repr=False)`).

### 1-2. `profiles/` 8개가 TODO/placeholder 마커를 보유한다

`aapl.yaml`(7) · `cvx.yaml`(7) · `chtr.yaml`(7) · `jnj.yaml`(7) · **`nvda.yaml`(7)** · `spcx.yaml`(7) · `pfe.yaml`(7) · `035420.yaml`(1) — 총 50건

### 1-3. `profiles/nvda.yaml`은 **낡은 주석이 아니라 진짜 자동 생성 스텁**이다

```yaml
# NVIDIA CORP — Auto-generated draft profile          ← 1행
# Source: SEC EDGAR | Generated by valuation-tool
# TODO: Add segment data, multiples, and scenario parameters
...
  analysis_date: "2026-07-11"                          ← 최근 주간 실행
segments:
  MAIN:
    name: "Main Business"
    multiple: 10.0   # TODO: Set appropriate EV/EBITDA multiple   ← 제너레이터 기본값
peers: []
  # TODO: Add peer companies                          ← 피어 0건
```

단일 `MAIN` 세그먼트, 제너레이터 기본 배수 10.0, 피어 0건. **큐레이션이 한 번도 적용되지 않은 상태다.**

### 1-4. 그런데 git에는 NVDA 큐레이션 작업이 남아 있다

| 커밋 | 내용 |
|---|---|
| `eaf2dfa` | feat(profiles): **NVDA 3중 앵커 재밸류에이션 (Q1 FY27 10-Q)** + CODEX 핸드오프 2건 |
| `18c5d5e` | fix(profiles): NVDA TTM 원문 정오 교정 + 보고서 2차 평가 반영 |
| `3006899` | fix(output/profiles): sensitivity 라벨 + **NVDA 프로파일 정제** |
| `a6b1356` | fix(profiles): **NVDA 피어 재감사** — 실체 없는 피어 2건 + 소멸 법인 1건 제거 |
| `6a37e2c` | docs(handoff): 보고서 재감사 잔여 1건 반영 — **배포 확정** |

> **큐레이션되고 리포트까지 배포된 NVDA 프로필이, 2026-07-11 주간 실행에 의해 스텁으로 덮어써진 것으로 보인다.**

`CLAUDE.md`가 정확히 이 위험을 명시한다:
> *"**`profiles/` is AI-regenerated, not a test fixture**: the weekly pipeline rewrites `profiles/*.yaml`"*

---

## 2. 요청 ① — 출력 델타 실측 (유료 호출 0건)

**`scripts/r16_profile_delta.py`를 실행하고 결과를 이 문서에 첨부할 것.**

```bash
PYTHONIOENCODING=utf-8 python scripts/r16_profile_delta.py
```

READ-ONLY다. 파일을 쓰지 않고 네트워크/LLM을 호출하지 않는다. 각 프로필에 대해:

| 컬럼 | 의미 |
|---|---|
| `draft(old→new)` | 게이트 `text=""`(R12 이전) vs `text=profile_text`(R12 이후) |
| `quality(pre→post)` | 게이트 적용 **전** 등급/점수 → **후**(F/0 재계산 반영) |
| `text로 새로 걸린 blocker` | `no_todo_markers` / `no_placeholder_multiples` |

**가장 중요한 숫자는 이것이다:**

> **`🔴 text 때문에 새로 draft가 된 것`** = R12가 없었다면 **확신 있는 밸류에이션이 나갔을** 프로필 목록.
> **이게 곧 "게이트가 죽어 있던 동안의 실제 노출"이다.**

(이미 `dcf_vs_peer`(피어 0건) 등으로 걸리고 있던 프로필은 R12와 무관하게 draft였다 — 이 둘을 구분해야 문제의 크기를 정직하게 말할 수 있다.)

**샌드박스 주의:** 이 저장소는 Linux 셸 마운트에서 **`valuation_runner.py`가 2298줄 대신 2291줄로 잘려 보인다**(재현됨). 셸에서 실행/검증하지 말고 **Windows 로컬 인터프리터**로 돌릴 것. 파일 무결성 판단은 파일 도구 기준. (`DEBATE_e1_round1.md` Round 4, `PLAN_E1` 부록 참조)

---

## 3. 요청 ② — 큐레이션 유실 구조 (설계 판정)

주간 실행이 큐레이션 산출물을 덮어쓴다면, **큐레이션은 영구히 휘발한다.** 리포트를 배포한 프로필조차 다음 주에 스텁으로 돌아간다.

**Q1.** `profiles/nvda.yaml`이 정말 주간 실행에 덮어써졌는가? (`git log -p -- profiles/nvda.yaml` — **읽기만**)
**Q2.** 보호 메커니즘은 무엇이어야 하는가?
  - (a) `curated: true` 플래그 — 제너레이터가 해당 파일을 건너뜀
  - (b) `profiles/curated/` 디렉터리 분리 — 주간 실행은 `profiles/auto/`만 씀
  - (c) 주간 실행이 스텁을 쓰기 전에 기존 파일이 non-draft면 **거부 + 경고**
  - (d) 기타
**Q3.** 이미 유실된 큐레이션(NVDA 등)을 복구할 것인가? 복구한다면 **어느 커밋 기준**인가?

> 🔴 **`git checkout` / `git restore` / `git show HEAD:<f> > <f>` 금지.** 작업 트리에 미커밋 작업이 대량으로 있다 (`CLAUDE.md` Session Safety). 복구가 필요하면 **git blob을 임시 경로로 추출해 대조**한 뒤 수동 병합할 것.

---

## 4. 요청 ③ — `_TODO_PATTERNS` 오탐 리스크

```python
# engine/investability_gate.py:40
_TODO_PATTERNS = ("todo", "auto-generated draft profile", "fixme", "placeholder")
```

이제 이 패턴이 **YAML 원문 전체를 부분 문자열 매칭**한다.

**`"placeholder"`는 위험하다.** 큐레이션된 프로필의 `notes:` / `rationale:` 필드에 그 단어가 우연히 들어가면 → `no_todo_markers` 실패 → `draft: true` → **quality F/0 오탐**.
예: `notes: "peer set is final; no placeholder multiples remain"` → **F/0**.

**Q4.** 마커 매칭을 앵커링할 것인가?
  - (a) 주석 라인(`#`로 시작)에서만 검색
  - (b) 전용 필드(`draft: true` / `generated: auto`)만 신뢰하고 텍스트 휴리스틱은 **warn**으로 강등
  - (c) 패턴에서 `"placeholder"` 제거 (10.0 배수 검출은 구조적으로 이미 하고 있다)

---

## 5. 요청 ④ — 주간 파이프라인이 스텁을 배포하지 못하게

**Q5.** `scheduler/`가 draft 프로필의 결과를 **네이버 포스팅/리포트로 내보내는가?**
그렇다면 게이트가 `draft: true`로 표시한 결과는 **배포 경로에서 차단**되어야 한다.
현재는 게이트가 `result.draft = True` + quality F/0을 **표시만** 하고, 배포를 막는지는 확인되지 않았다.

---

## 6. 우선순위 (작성자 의견)

| # | 항목 | 근거 |
|---|---|---|
| **1** | **§2 델타 실측** | 문제의 크기를 모르면 나머지는 전부 추측이다. 유료 호출 0건, 즉시 가능 |
| **2** | **Q5 배포 차단** | 스텁이 외부로 나가는 것이 최악의 결과다 |
| **3** | **Q2 큐레이션 보호** | 안 고치면 큐레이션은 매주 휘발한다 |
| **4** | **Q4 오탐 앵커링** | 게이트가 오탐을 내면 결국 무시된다 |
| 5 | Q3 유실 복구 | 위 4개가 정리된 뒤 |

**E1(유료 측정)은 이것과 병행하지 않는다.**
E1은 "우리가 몇 콜을 쓰는지 모른다"를 고쳤다. **R16은 "우리가 무엇을 밸류에이션하고 있는지 모른다"이다.**

---

## 7. Codex 실측·판정 (2026-07-12)

### 7-1. 출력 델타 실측

Windows 로컬 Python에서 `PYTHONIOENCODING=utf-8`로 `scripts/r16_profile_delta.py`를 실행했다. 파일 쓰기·네트워크·LLM 호출은 0건이다.

| 항목 | 결과 |
|---|---:|
| 정상 분석된 프로필 | 45 |
| R12 이전에도 text 외 blocker로 draft | 33 |
| **text 때문에 old=False → new=True로 뒤집힘** | **1 (`035420.yaml`)** |
| gate 이후 quality가 신규 F/0 | 34 |
| 로드 실패 | 2 (`346010.yaml`, `_template.yaml`: `shares_total=0`) |

TODO/placeholder 마커가 있던 8개 중 결과는 다음과 같다.

| profile | draft(old→new) | quality(pre→post) | text 신규 blocker |
|---|---|---|---|
| `035420.yaml` | **False→True** | C/63→F/0 | `no_todo_markers` |
| `aapl.yaml` | True→True | B/75→F/0 | TODO + placeholder multiple |
| `chtr.yaml` | True→True | B/75→F/0 | TODO + placeholder multiple |
| `cvx.yaml` | True→True | B/75→F/0 | TODO + placeholder multiple |
| `jnj.yaml` | True→True | C/58→F/0 | TODO + placeholder multiple |
| `nvda.yaml` | True→True | B/70→F/0 | TODO + placeholder multiple |
| `pfe.yaml` | True→True | B/75→F/0 | TODO + placeholder multiple |
| `spcx.yaml` | True→True | D/53→F/0 | TODO + placeholder multiple |

**해석:** R12가 판정 자체를 새로 뒤집은 실제 노출은 1개뿐이다. 나머지 33개는 DCF/peer 등 기존 blocker로 이미 draft였지만, R13 이전에는 quality가 F/0으로 동기화되지 않아 출력상 확신도가 과장됐다. `035420.yaml`은 TODO가 아니라 헤더의 `Auto-generated draft profile (enhanced)` 문구 하나로 걸렸으며, 구조는 MAIN 15.0x로 보강돼 있어 **텍스트 휴리스틱 오탐 가능성의 실제 사례**다.

### 7-2. Q1 — NVDA 유실 원인

**큐레이션 유실은 확정, “weekly run이 덮었다”는 주체 귀속은 미확정이다.**

- HEAD 최신 상태는 `a6b1356`의 큐레이션 프로필(5개 세그먼트, peer 다수)이다.
- 현재 `profiles/nvda.yaml`은 미커밋 수정이며 `91 insertions / 259 deletions`, 단일 MAIN 10.0x, peers 0건인 `_generate_draft_profile()` 템플릿과 일치한다.
- 현재 파일 mtime은 `2026-07-11 22:31:09`, 내용의 `analysis_date`/reconciliation `as_of`도 7월 11일이다.
- 그러나 `logs/weekly_20260711.log` mtime은 12:41이고 최근 weekly summary에 NVDA가 없다.

따라서 7월 11일 야간의 `auto_fetch()`/`auto_analyze()` 계열 실행이 overwrite한 것은 확실하지만, weekly scheduler인지 수동 `--company NVDA --auto`인지 현재 artifact만으로는 판별할 수 없다.

### 7-3. Q2 — 큐레이션 보호 설계

**권고: (c)를 기본 안전장치로 즉시 적용하고 (a)를 명시적 메타데이터로 병행한다.**

1. 기존 destination이 존재하고 non-draft/curated로 판정되면 `_generate_draft_profile()`은 overwrite를 거부하고 경고한다.
2. 신규 자동 스텁은 `draft: true`, `generated: auto`를 구조화 필드로 기록한다.
3. 수동 큐레이션 완료 시 `curated: true`로 승격한다. `curated: true`는 자동 overwrite 절대 금지다.
4. 최신 원천 데이터는 기존 curated 파일에 즉시 덮지 말고 staging 파일에 생성해 사람이 diff/merge한다.

디렉터리 전면 분리(b)는 장기적으로 깔끔하지만 현재 모든 CLI/profile 경로를 바꾸므로 1차 안전 패치로는 과하다. `curated` 플래그만 단독 사용하면 아직 플래그가 없는 기존 큐레이션을 보호하지 못하므로 (c)가 반드시 함께 있어야 한다.

### 7-4. Q3 — NVDA 복구 기준

**복구 권고 기준은 `a6b1356`**이다. 최신 큐레이션과 peer 재감사를 모두 포함한다. 다만 그대로 되돌리지 말고 임시 blob과 현재 파일을 대조하여 다음만 수동 병합한다.

- 큐레이션 구조·시나리오·peer: `a6b1356` 기준
- 현재 raw data 중 검증 가능한 최신 항목: shares, 2026 consolidated, analysis date, reconciliation 결과
- 자동 생성 MAIN/10.0/empty peers는 폐기

보호 메커니즘과 배포 차단이 먼저이며, 이 문서 판정 단계에서는 복구를 실행하지 않았다.

### 7-5. Q4 — TODO 오탐

**권고: (b)를 최종 계약으로 채택하고, migration 기간에는 (a)+(c)를 보조로 사용한다.**

- blocking의 authoritative source는 `draft: true`, `generated: auto`, `curated: true` 같은 구조화 필드여야 한다.
- legacy 텍스트 휴리스틱은 `# TODO:` / `# FIXME:`처럼 주석 라인에 앵커링한다.
- 일반 단어 `placeholder`와 `auto-generated draft profile` 부분 문자열은 blocking에서 제거한다. `035420.yaml`이 실제 오탐 사례다.
- 10.0x 자체는 합법적 배수일 수 있으므로 단독 block하지 않고 `generated:auto` 또는 명시적 TODO와 결합한다.

### 7-6. Q5 — draft 외부 배포 여부

**현재 draft는 외부 배포될 수 있다. 차단 로직이 없다.**

- `AnalyzeResult`는 `result`를 보유하지만 weekly summary entry에는 `draft`/quality를 복사하지 않는다.
- weekly는 `analyze_result`가 존재하면 무조건 `status="success"`로 기록한다.
- Storage 업로드는 `status == "success"`만 확인한다.
- WordPress와 Naver도 `status == "success"`만 필터링하며 draft/quality F를 검사하지 않는다.
- 이후 email, Storage, WordPress, Naver 단계가 순차 실행된다.

**필수 차단점:** `_run_valuation()`에서 `analyze_result.result.draft` 또는 quality F를 확인해 `status="draft_blocked"`로 기록하고, Excel 외부 업로드·email·WordPress·Naver의 성공 집계에서 제외해야 한다. 원인은 summary에 blockers와 함께 남겨 내부 검토는 가능하게 한다.

### 7-7. 최종 우선순위

1. scheduler `draft_blocked` 외부 배포 차단
2. generator overwrite 거부(c) + structured `draft/generated/curated` 메타데이터(a)
3. 텍스트 휴리스틱을 구조화 필드 중심으로 축소
4. NVDA를 `a6b1356` 기준으로 수동 복구·최신 raw data 병합
5. 346010/profile template의 `shares_total=0` 진단 도구 처리 정책 정리

**유료 API 호출: 0건. 파일 복구 및 보호 코드 구현: 아직 미착수.**

---

## 8. 구현 상태 (2026-07-12)

사용자 승인 후 다음을 구현했다.

- weekly `draft_blocked` 내부 기록 + Storage/email/WordPress/Naver 외부 발행 제외
- 전부 draft일 때 발행 0건을 조용히 넘기지 않고 `_alert()`로 blocked 수와 회사별 사유 통지
- 신규 auto stub의 `draft/generated/curated` 구조화 metadata
- curated/non-draft destination overwrite 거부 + `profiles/staging/` 생성
- TODO/FIXME 주석만 legacy 보조 검사; 일반 placeholder/header 부분 문자열 block 제거
- R18 peer 생성 실패를 `HANDOFF_CODEX_r18_peer_generation.md`로 별도 등록

NVDA 복구와 `346010.yaml` 수정은 보호 장치 검증 이후 별도 작업으로 남겼다. 유료 API 호출은 0건이다.

**검증:** 전체 suite `810 passed, 5 deselected` (제외 5개는 AGENTS.md에 명시된 AI 재생성 profile fixture 그룹).
