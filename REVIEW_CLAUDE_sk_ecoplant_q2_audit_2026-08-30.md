# SK에코플랜트 2026 Q2 산출물 감사 — 수정 체크리스트

작성: 2026-08-30 (Claude, Cowork)
대상: `valuation-results/2026-08-23-sk-ecoplant-q2-update/` 전체 · `profiles/sk_ecoplant_2026h1.yaml` · 관련 엔진 경로
기준 커밋: `552453c` (main)
검증 방법: 프로파일 입력에서 독립 재계산 → 리포트/엑셀 대조. 재현 스크립트는 §6.

---

## 0. 요약

산술은 전부 재현된다. `facts.py::validate()` PASS, 부문 합계 = 연결 합계, 순차입금·주식수 브리지 일치.
확률가중 46,228원도 정확히 재현된다.

**그러나** (a) 엑셀 딜리버러블에 리포트 결론과 2.7배 모순되는 패널이 있고, (b) 엔진 버그 2건이 산출물에 실제로 반영됐으며, (c) 시나리오 스프레드의 상당 부분이 사업 판단이 아니라 모델링 아티팩트다.

세 가지 구조 수정(D1·M1·V1)을 적용하면 **점추정은 46,228 → 41,691원(−10%)인데 밴드는 180,805 → 99,189원으로 45% 좁아진다.** 즉 극단적 스프레드는 견해차가 아니라 설계 문제였다.

| 등급 | 건수 | 의미 |
|---|---:|---|
| P0 | 3 | 제출물에 그대로 나가면 즉시 발각되는 모순 |
| P1 | 1 | 엔진 버그 — 수정 완료, Codex 검증 대기 |
| P2 | 6 | 방법론·설계 — 정책 라운드 후 반영 |
| P3 | 4 | 표기·정합 |

> **2026-08-30 개정**: 초판에서 E1(`allocate_da`의 `ev_revenue` 제외)을 P1 버그로 분류했으나 **오분류였다.**
> 해당 동작은 `tests/test_engine.py::test_allocate_da_excludes_ev_revenue`가 명시적으로 검증하는 의도된 설계다.
> 패치를 되돌리고 P2(§3 V5)로 재분류했다. 경위는 §3 V5.

---

## 1. P0 — 엑셀 딜리버러블 모순 (제출 전 필수)

### X1. Dashboard "멀티플 교차검증" 패널이 NCI·RCPS를 차감하지 않는다

- **위치**: `SK에코플랜트_밸류에이션_모델.xlsx` → `Dashboard` 시트 "멀티플 교차검증", `SOTP Valuation` 시트 "Equity Bridge (Mixed SOTP)"
- **증상**: 브리지가 `EV − 순차입금`에서 끝난다. 비지배지분 3,674,164과 RCPS 534,200이 빠져 있다.

| 방법론 | 엑셀 표기 | 완전 브리지 | 과대 |
|---|---:|---:|---:|
| SOTP (Mixed) | 118,487원 | 51,831원 | +66,656 |
| DCF (FCFF) | 126,479원 | 59,823원 | +66,656 |
| EV/Revenue | 73,241원 | 6,585원 | +66,656 |
| P/BV | 100,091원 | 41,896원 | +58,195 |

- **왜 심각한가**: 리포트 §2.3이 "비지배지분 차감을 바로잡은 것이 이번 개정의 가장 큰 구조적 변화"라고 명시했다. 그 수정이 **시나리오 시트에만 적용되고 SOTP·Dashboard에는 반영되지 않았다.** 엑셀을 연 사람은 리포트 결론(43,032원)의 2.7배를 먼저 본다.
- **조치**: `output/` 교차검증 시트의 equity bridge를 `calc_scenario`와 동일한 차감 항목으로 통일. 리포팅 경계 규칙(`.claude/rules/reporting-boundary.md`) 위반 — 엔진 계산을 output이 복제한 4번째 사례다.
- **상태**: ☐

### X2. P/BV 교차검증이 총자본을 쓴다 (NCI 이중 계상)

- **위치**: 동일 패널, P/BV 행
- **증상**: `총자본 6,319,333 × 1.0x` → 100,091원. **지배기업 소유주지분은 2,645,169**이다 (`facts.py::BALANCE_SHEET["parent_equity"]`).
- **정정**: 41,896원. P/BV는 정의상 지배주주 장부가 기준이다.
- **조치**: `calc_pbv` 호출부가 `equity`가 아니라 `parent_equity`를 받도록. 프로파일에 `parent_equity` 필드 추가 필요(Optional·기본값 = `equity`로 하위호환).
- **상태**: ☐

### X3. `eco_frontier` 필드에 NCI를 넣어 라벨이 틀렸다

- **위치**: `profiles/sk_ecoplant_2026h1.yaml:eco_frontier: 3674164` → 엑셀 `Assumptions` 시트 **"에코프론티어 파생상품부채 3,674,164"**, `Scenario Analysis` 시트 **"(-) 기타 차감"**
- **증상**: EV의 33%를 차지하는 최대 차감 항목이 무관한 회사의 파생상품부채로 표기된다. 프로파일 주석은 이 유용을 인정하고 있다.
- **조치**: `ValuationInput`에 `minority_interest` 필드 신설(Optional, 기본 0), 라벨 "(-) 비지배지분". `eco_frontier`는 하위호환용으로 존치.
- **상태**: ☐

---

## 2. P1 — 엔진 버그 (수정 완료)

### E2. Monte Carlo가 음수 지분에도 DLOM을 적용한다  ✅ 패치 완료

- **위치**: `engine/monte_carlo.py` — `ps *= 1 - dlom_samples / 100` (부호 가드 없음)
- **정책 위반**: `.claude/rules/engine.md`가 **"DLOM is not applied to negative equity"**를 명시하고 `engine/scenario.py::calc_scenario`는 이를 지킨다. MC만 어긴다.
- **방향**: 음수에 할인을 곱하면 손실이 **줄어든다**. `monte-carlo.md`의 "Do NOT clamp — upward-biases statistics" 취지와 정면으로 어긋난다.
- **왜 안 잡혔나**: 기존 `test_negative_equity_clamped_to_zero`가 `dlom_mean=0`으로 돌아 DLOM 경로를 전혀 타지 않는다.
- **수정**: `ps = np.where(ps > 0, ps * (1 - dlom_samples / 100), ps)`
- **신규 테스트**: `test_dlom_not_applied_to_negative_equity` — 전 표본 음수 조건에서 `dlom_mean=0`과 `40`의 결과가 동일해야 하고, 양수 통제군에서는 달라야 한다. **패치 전 코드에서 정확히 실패**(min −290,012 vs −483,354)하고 패치 후 통과한다.
- **회귀 (실측, 프로파일 47종)**: 7종만 변동, 전부 음수 꼬리에서 보수적 방향. `weighted_value`는 전 종목 불변.

| 프로파일 | 음수% | mean 전 → 후 | min 전 → 후 |
|---|---:|---:|---:|
| sk_ecoplant_2026h1 | 3.1 | 47,527 → 47,482 | −26,797 → **−33,275** |
| sk_ecoplant (Q1) | 0.6 | 37,144 → 37,141 | −9,274 → −11,607 |
| 005380 | 97.8 | −293,388 → −299,241 | −542,929 → −551,017 |
| 006260 | 0.2 | 249,757 → 249,756 | −44,291 → −47,534 |
| 456040 | 0.1 | 29,844 → 29,844 | −5,702 → −5,903 |
| gm | 53.9 | 2 → 2 | −75 → −77 |
| mrna | 100.0 | −50 → −51 | −82 → −82 |

- **부수 관찰 (범위 외)**: `005380`은 MC 표본의 **97.8%가 음수 지분**이다. 프로파일 입력 결함 가능성 — 별도 확인 필요.
- **상태**: ☑ 패치 완료 · 전체 `pytest tests/` **1110 passed** · Codex 검증 대기

### E3. MC가 시나리오 Base와 다른 입력셋으로 돈다

- **위치**: `valuation_runner.py` MC 호출부 — `seg_ebitdas`가 시나리오 오버라이드가 아닌 SOTP 시트 기준값을 받는다
- **증거**: 엑셀 `Dashboard` "MC 입력 분포 가정"이 배수 **5.84x / 9.73x / 7.57x**를 찍는다. Base 시나리오는 **6.0 / 10.0 / 8.0**이다. EBITDA도 1,009,557 / 259,300 / 290,176 vs 1,050,000 / 220,000 / 280,000으로 다르다.
- **왜 문제인가**: 리포트 §6.4가 "MC 중위값 42,768이 Base 43,032과 가깝다"를 교차검증처럼 쓴다. 서로 다른 입력셋 두 개가 우연히 비슷한 EV(11.22조 vs 11.14조)를 낸 것일 뿐이다.
- **부수 문제**: MC가 **EBITDA를 확률변수로 쓰지 않는다.** 밴드를 만드는 최대 동인(ALC 0.55~1.60조, 2.9배)이 MC에선 상수다. ALC EBITDA에 ±30%만 넣어도 P5 15,811 → **−2,282**, 음수 비중 0.3% → 5.8%.
- **조치**: 정책 라운드에서 (a) MC 기준을 Base 시나리오와 동기화할지, (b) `segment_ebitda` 분포를 MC 입력에 추가할지 결정.
- **상태**: ☐ 정책 라운드 (E1·E2와 분리)

---

## 3. P2 — 방법론 (정책 확정 후 반영)

### V1. NCI를 전 시나리오에 장부가로 고정했다

- 시장기준 EV에서 **장부가** NCI를 차감하는 것 자체도 문제지만, 더 큰 문제는 자회사 EV가 3.25배 흔들리는 동안 그 자회사 소수주주 청구권만 3,674,164에 못 박혀 있다는 점이다.
- Bull 과소차감 / Bear 과대차감 → **밴드가 인위적으로 벌어진다.**
- EV 비례(Base NCI/EV = 33.0% 고정) 적용 시: Bull 141,423 → **109,811**, Bear −39,382 → **−9,916**. 점추정은 46,228 → 45,798원으로 거의 불변.
- **조치**: 부문별 지분율 기반 NCI 차감이 정석이나 공시 한계가 있다. 최소한 "NCI 고정이 밴드를 부풀린다"를 §10 한계에 명시.
- **상태**: ☐

### V2. DLOM을 시나리오 변수로 썼다 (10 / 15 / 25%)

- DLOM은 유동성 할인이지 펀더멘털 변수가 아니다. 영업 시나리오와 이중계상되고, "사업이 잘 되면 장외 유동성이 좋아진다"는 가정이 암묵적으로 들어간다.
- 전 시나리오 15% 고정 시: Bull 141,423 → 133,566, 가중 46,228 → 44,656원.
- **부수**: Bear의 −39,382원은 DLOM **미적용** 값(정책상 정확)인데 리포트 §0 표에서 Base·Bull의 DLOM **적용** 값과 같은 열에 놓인다. 혼합 기준임을 각주로 밝혀야 한다.
- **상태**: ☐

### V3. Bull이 피크 이익에 프리미엄 배수를 얹었다

- ALC TTM EBITDA 1.60조 중 **사실상 94%가 H1 2026**이다 (H1 ALC 영업이익만 1,435,703).
- Bull은 이 피크를 영구화하면서 배수를 6.0x → **7.0x로 올렸다.** 정석은 반대다 — 사이클 피크 이익에는 **낮은** 배수를 쓴다.
- 7.0x → 6.0x만 해도 가중 46,228 → **41,666원 (−10%)**.
- **상태**: ☐

### V4. 스프레드 가드레일이 한쪽 레버만 막는다 ★

- `valuation_runner.py:353` `_SOTP_MAX_RATIO = 2.0`은 **`segment_multiples`에만** 걸린다. `segment_ebitda` · `segment_revenue`에는 상한이 없다.
- `engine/scenario_validator.py`는 `bull_ev/bear_ev >= 1.3` **하한만** 검사한다. 상한이 없어 3.25배도 10배도 통과한다.
- **이 프로파일이 정확히 그 틈을 밟는다**:

| 레버 | Bull/Bear 비율 | 클램프 |
|---|---:|---|
| `segment_multiples` ALC | 1.56x | ≤2.0x 적용됨 (통과) |
| `segment_multiples` SOL | 2.00x | ≤2.0x 적용됨 (경계) |
| **`segment_ebitda` ALC** | **2.91x** | **없음** |
| 합성 EV | **3.25x** | **없음** |

- **분해**: EBITDA만 2.25x × 배수만 1.45x = 3.26x. **완전 곱셈 — 상쇄가 0이다.** 8개 레버가 전부 같은 방향으로 100% 상관됐다.
- `.claude/rules/engine.md`는 "Bull/Bear EV gap > 2x는 over-fit 신호"라고 적어놓고 코드로는 강제하지 않는다.
- **조치 제안**: `segment_ebitda`/`segment_revenue`에도 동일 클램프, 또는 `validate_scenario_differentiation`에 EV 상한 추가. 기존 `curated` + `allow_wide_scenario_spread` opt-in 패턴 재사용.
- **상태**: ☐ 정책 라운드

---

### V5. `allocate_da`가 `ev_revenue` 부문을 D&A 배분 분모에서 제외한다 (초판 오분류 정정)

- **위치**: `engine/sotp.py::allocate_da` — `ev_codes`를 `method == "ev_ebitda"`로만 구성하고 연결 D&A **100%**를 그 부문들에만 배분한다.
- **초판 판단**: 버그(P1). **정정**: 의도된 설계다. 근거 두 가지 —
  1. `tests/test_engine.py::test_allocate_da_excludes_ev_revenue`가 이 동작을 이름으로 못박아 검증한다.
  2. `.claude/rules/engine.md:49`가 "D&A re-allocation alone yields near-zero EBITDA for formerly-excluded segments"로 이 전제를 명시한다.
  → 패치를 시도했다가 위 테스트와 `test_revenue_to_ebitda_transition` 2건이 깨져 **되돌렸다.**
- **그럼에도 남는 문제**: 이 설계는 **pre-profit 옵셔널리티 부문**(Tesla FSD, Google Quantum, Meta AI Platform — 전부 `assets: 0`)을 위한 것이다. SK에코플랜트 Solution은 그 유형이 아니다. **유무형자산 1,205,254 · 매출 4.0조의 성숙·자산집약 부문이 적자라는 이유만으로 `ev_revenue`를 쓰고 있을 뿐**이고, 실제 감가상각이 연결 D&A에 들어 있다. 엔진은 둘을 구별할 방법이 없다.
- **영향 (실측)**: Solution 자산이 전체의 25.6%인데 몫 0을 받고 그 D&A가 HI/GAS/ALC로 재배분된다.

| 부문 | 현행 D&A | 전부문 배분 시 | 현행 EBITDA | 정정 EBITDA |
|---|---:|---:|---:|---:|
| HI | 40,176 | 29,874 | 290,176 | 279,874 (−3.6%) |
| GAS | 134,300 | 99,863 | 259,300 | 224,863 (−13.3%) |
| ALC | 109,557 | 81,464 | 1,009,557 | 981,464 (−2.8%) |
| SOL | 0 | 72,832 | (미사용) | (미사용) |

  → SOTP 시트 EV 11.217조 → **10.640조 (−5.1%)**, 완전브리지 주당 51,831 → **42,690원**

- **엑셀 자체 모순**: `Financial Summary` 시트 합계 행이 유무형자산 4,700,289를 찍는데 자산비중 합은 3개 부문으로 1.0이다. 표가 스스로 어긋난다.
- **폭발 반경 (실측)**: `ev_revenue` 혼합 프로파일 8종 중 4종만 영향 — meta(15.4%) · nexus(100%) · nvo_sotp(21.0%) · sk_ecoplant_2026h1(25.6%). googl·nvda_ttm·nvda_fy27e·tsla는 옵셔널리티 부문 `assets: 0`이라 diff 0. `pbv` 부문까지 확대하면 7203_t(42.6%)가 추가된다.
- **제안 설계**: 기본 동작은 유지하고 세그먼트 단위 opt-in을 추가한다 — `segments.<CODE>.da_pool: true` (Optional, 기본값 = `method == "ev_ebitda"`). 신규 YAML 필드는 Optional·기본값 규약을 따르므로 하위호환이 유지되고, 기존 테스트 2건도 그대로 통과한다. `sk_ecoplant_2026h1`은 `SOL.da_pool: true`.
- **상태**: ☐ 정책 라운드 — **패치 금지, 설계 확정 후 구현**

## 4. P3 — 표기·정합

### R1. 리포트 §6.2 "DLOM 전 50,661원"이 모델값과 다르다
- 모델·엑셀 모두 **50,626원**. 43,032 = 50,626 × 0.85이므로 50,661이 틀렸다. 35원 오타. **상태**: ☐

### R2. §6.4 MC 프레이밍
- "MC가 SOTP를 검증했다"는 서술 삭제. SOTP·DCF·MC 셋 다 같은 정상화 EBITDA 판단(연결 op 1,155,000)에 걸려 있어 독립 검증이 아니다. DCF EBITDA 1,439,034 = 1,155,000 + 284,034로 동일 소스다. **상태**: ☐

### R3. README가 Q1 모델을 설명한다
- README는 아직 IPO 성공/실패 시나리오, CPS 6,000억, **ALC 13.0x**를 적는다. Q2 리포트는 "CPS 상환 여부는 더 이상 시나리오 변수가 아니다"이고 ALC는 6.0x다. 레포와 리포트를 함께 제출하면 버전 불일치가 보인다. **상태**: ☐

### R4. 프로파일의 출처 혼재 (권고)
- `segment_data.2026.op`(250,000/125,000/900,000/−120,000)과 `consolidated.2026.op`(1,155,000)·`net_income`(500,000)은 **정상화 추정치**이고, 같은 블록의 revenue·assets·equity·borrowings는 **공시 실측치**다. H1 실제 영업이익만 1,465,008이므로 명백히 다른 성격이다.
- 프로파일 주석에 언급은 있으나 필드 단위 provenance 태그가 없다. 사용자 본인 원칙("원문 인용 범위를 별도 `_sources` 파일로 고정")과 어긋난다. **상태**: ☐

---

## 5. 수정 효과 시뮬레이션

| 적용 | Base | Bull | Bear | 확률가중 |
|---|---:|---:|---:|---:|
| 현행 | 43,032 | 141,423 | −39,382 | **46,227** |
| V2 (DLOM 15% 고정) | 43,032 | 133,566 | −39,382 | 44,656 |
| V1 (NCI를 EV 비례) | 43,032 | 109,811 | −9,916 | 45,798 |
| V3 (Bull ALC 7.0→6.0x) | 43,032 | 118,615 | −39,382 | 41,666 |
| **V1+V2+V3 누적** | **43,032** | **89,273** | **−9,916** | **41,691** |

**점추정 −10%, 밴드 −45%.** Base 43,032원은 세 수정에도 미동이 없다 — 사업 판단이 아니라 구조가 스프레드를 만들었다는 뜻이다.

### 확률배분 민감도 (현행 기준)

| Bull / Bear | 가중 | 기준 대비 |
|---|---:|---:|
| 30% / 10% | 64,308 | +39.1% |
| 20% / 20% | **46,227** | — |
| 10% / 30% | 28,147 | −39.1% |

Base 확률을 40~80%로 흔들면 44,630~47,825원(±3%)뿐이다. **60%짜리 Base는 사실상 장식이고, 답을 정하는 건 20:20이라는 꼬리 배분이다.**
기여도: Base(60%) 55.9% · **Bull(20%) 61.2%** · Bear(20%) −17.0% — 확률 1/3짜리가 더 크게 기여한다.

### 장외가 부합에 대한 판정

V1+V2+V3 적용 후 46,500원을 다시 맞추려면 **Bull 확률 25%가 필요하다.** 원래의 46,228 ≈ 46,500 일치는 검증된 수렴이 아니라 **부풀려진 두 꼬리가 상쇄된 잔차**였다. 리포트 §7이 "일치가 적정가치 확정을 뜻하지 않는다"고 쓴 것은 맞으나, 근거가 생각보다 더 약하다.

---

## 6. 재현

```bash
# 시나리오 산술 재현 · 스프레드 분해 · 확률 민감도
python3 scripts/audit_sk_ecoplant_q2.py

# 엑셀 패널 대조
python3 -c "
import openpyxl
wb=openpyxl.load_workbook('valuation-results/2026-08-23-sk-ecoplant-q2-update/SK에코플랜트_밸류에이션_모델.xlsx',data_only=True)
for r in wb['Dashboard'].iter_rows(values_only=True):
    c=[str(x) for x in r if x is not None]
    if c: print(' | '.join(c))"
```

---

## 7. 진행 순서

1. **패치 B(E2)** — 엔진 버그. ☑ 수정·테스트·회귀 완료 → **Codex 검증 중** (`HANDOFF_CODEX_sk_ecoplant_q2_audit_2026-08-30.md`)
2. **정책 라운드** — E3 · V1 · V2 · V3 · V4 · **V5** · X2(스키마 신설) · X3(스키마 신설). **Codex 정책 확정본 회신 후 구현** (`.claude/rules/codex-cross-review.md` 루프).
3. **X1 재생성** — output 브리지 통일 후 엑셀·리포트 재생성.
4. **R1~R4** — 문서 정정.

**제출용 산출물은 1~3이 끝나기 전까지 배포 보류.**

### 이번 라운드에서 확정된 것

- 리포트·엑셀의 모든 시나리오 산술이 프로파일 입력에서 **독립 재현**된다 (§5 스크립트).
- 출하된 Monte Carlo도 **정확히 재현**된다: baseline 실행 결과 mean 47,527 · median 42,768 · p5 3,909 · p95 107,065 · 음수 3.1% — 리포트 §6.4 표와 전 항목 일치. Q1 모델의 35,066원도 재현된다.
- 따라서 남은 지적은 전부 **계산 오류가 아니라 설계·표기 문제**다.
