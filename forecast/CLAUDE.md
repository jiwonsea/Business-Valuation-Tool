# Forecast memory pointer

The earnings-forecast engine is now the `forecast/` layer of the parent repository.
Current scoped instructions: `../.claude/rules/forecast.md`.
Usage and architecture: `README.md`.
Historical standalone-project context remains available in Git history and `HANDOFF_*` records.
